# Maven build output
target/

# Demo PKI material (generated by certs/setup-ca.sh and certs/generate-client-cert.sh)
certs/ca/*.key
certs/ca/*.crt
certs/ca/*.srl
certs/clients/
server/certs/*.p12

# Legacy paths (removed — kept for safety if old files remain locally)
client/certs/clients/
client/certs/*.key
client/certs/*.crt
client/certs/*.p12
client/certs/*.pem

# IDE
.idea
.vscode
*/target